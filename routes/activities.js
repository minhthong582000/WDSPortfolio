// var express = require('express');
// var router = express.Router();

// var controller = require('../controller').activityController;

// /* GET users listing. */
// router.route('/')
// // .get(controller.getActivities) //Lấy mọi hoạt động
// .post(controller.postCreateActivity) //Tạo hoạt động

// //Thao tác hoạt động
// router.route('/:id')
// .get(controller.getActivityInfo) //ajax thông tin
// .post(controller.postEditActivity) //Chỉnh sửa thông tin
// .delete(controller.getDeleteActivity) 

// //Thao tác hình ảnh của từng hoạt động
// router.route('/:id/img') //Thêm
// .post(controller.postAddImgForActivity)

// //Xóa ảnh hoạt động
// router.route('/:id/:img') 
// .delete(controller.getDelImgForActivity)


// module.exports = router;
